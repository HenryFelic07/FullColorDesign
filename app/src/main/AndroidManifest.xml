<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.fullcolordesign"
    android:installLocation="auto">

    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/logo"
        android:label="@string/app_name"
        android:roundIcon="@id/icon"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.NoActionBar">
        <activity android:name=".activities.ShopDetailsActivity"></activity>
        <activity android:name=".activities.AddProductActivity" />
        <activity android:name=".activities.EditarPerfilVendedorActivity" />
        <activity android:name=".activities.EditarPerfilUsuarioActivity" />
        <activity android:name=".activities.MainUsuarioActivity" />
        <activity android:name=".activities.MainVendedorActivity" />
        <activity android:name=".activities.ContrasenaOlvidadaActivity" />
        <activity android:name=".activities.RegistroVendedorActivity" />
        <activity android:name=".activities.RegistroUsuarioActivity" />
        <activity android:name=".activities.LoginActivity" />
        <activity android:name=".activities.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="com.theartofdev.edmodo.cropper.CropImageActivity"
            android:theme="@style/Base.Theme.AppCompat" />
    </application>

</manifest>